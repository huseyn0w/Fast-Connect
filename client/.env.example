# Base URL of the signaling server (Socket.IO + PeerJS broker).
# Leave empty to use the same origin that serves the app.
VITE_SERVER_URL=http://localhost:5000

# Path the PeerJS broker is mounted under on the server (must match the server's PEER_PATH).
VITE_PEER_PATH=/peerjs
